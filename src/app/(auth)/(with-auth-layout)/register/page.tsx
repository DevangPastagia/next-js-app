export default function register() {
  // instead of http://192.168.0.145:3000/auth/login use http://192.168.0.145:3000/login
  // adding parenthesis () on folder (auth) mark as route group for batter structure work.
  return <h1>This is the register page</h1>;
}
